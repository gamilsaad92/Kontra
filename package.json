{
  "name": "kontra-starter",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "ui",
    "api"
  ],
  "scripts": {
    "dev": "concurrently -n UI,API -c green,blue \"npm run dev -w ui\" \"npm run dev -w api\"",
    "build": "npm run build -w ui && npm run build -w api",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.3.3"
  }
}