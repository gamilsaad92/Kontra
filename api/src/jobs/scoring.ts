export async function runNightlyScoring(): Promise<{ processed: number }> {
  console.log('[jobs] nightly predictive scoring started')
  // placeholder: compute scores here
  console.log('[jobs] nightly predictive scoring completed')
  return { processed: 0 }
}
